# Shadow Agent runs fully on-device with no configuration.
# Everything below is OPTIONAL. Leave it unset for the default local-only experience.

# Multi-device sync (optional). Set this to the base URL of a sync endpoint to
# enable encrypted model-state sync across devices. When unset, the app stays
# local-only and nothing leaves the browser.
# NEXT_PUBLIC_SYNC_URL=
